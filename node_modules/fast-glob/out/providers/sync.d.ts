import { Task } from 'fast-glob/out/managers/tasks';
import ReaderSync from 'fast-glob/out/readers/sync';
import { Entry, EntryItem, ReaderOptions } from 'fast-glob/out/types';
import Provider from 'fast-glob/out/providers/provider';
export default class ProviderSync extends Provider<EntryItem[]> {
    protected _reader: ReaderSync;
    read(task: Task): EntryItem[];
    api(root: string, task: Task, options: ReaderOptions): Entry[];
}
