/// <reference types="node" />
import { Readable } from 'stream';
import { Task } from 'fast-glob/out/managers/tasks';
import ReaderStream from 'fast-glob/out/readers/stream';
import { ReaderOptions } from 'fast-glob/out/types';
import Provider from 'fast-glob/out/providers/provider';
export default class ProviderStream extends Provider<Readable> {
    protected _reader: ReaderStream;
    read(task: Task): Readable;
    api(root: string, task: Task, options: ReaderOptions): Readable;
}
